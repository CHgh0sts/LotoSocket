import { NextResponse } from 'next/server'

/**
 * Placeholder : invitations (hors jeu) non implémentées.
 * Le client attend un tableau (longueur = nombre d’invitations en attente).
 */
export async function GET() {
  return NextResponse.json([])
}
