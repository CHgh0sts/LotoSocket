import { NextResponse } from 'next/server'

/**
 * Placeholder : demandes d’amis non implémentées.
 * Le client lit `receivedRequests` pour le badge.
 */
export async function GET() {
  return NextResponse.json({ receivedRequests: [] })
}
